version: 2.1
orbs:
    slack: circleci/slack@4.1.1
   
jobs:
  my_job:
    docker:
      - image: circleci/node
    steps:

      - run: echo $SLACK_DEFAULT_CHANNEL
      - slack/notify:
#           template: basic_fail_1
          event: always
          mentions: '@henna'
          custom: | 
            {
                "blocks": [
                    {
                        "type": "section",
                        "text": {
                            "type": "mrkdwn",
                            "text": "Failed: $CIRCLE_USERNAME's workflow (<$CIRCLE_BUILD_URL|$CIRCLE_STAGE>) in \n<https://app.circleci.com/pipelines/github/$CIRCLE_PROJECT_USERNAME/$CIRCLE_PROJECT_REPONAME|$CIRCLE_PROJECT_USERNAME/$CIRCLE_PROJECT_REPONAME> \n - Fix back link to case (<$CIRCLE_REPOSITORY_URL/$CIRCLE_SHA1|$CIRCLE_SHA1>) This message contains a URL http://foo.com/\nSo does this one: www.foo.com\nThis message contains a URL <http://foo.com/>\n<http://www.foo.com|This message *is* a link>\n<mailto:bob@example.com|Email Bob Roberts>"
                        }
                    }
                ]
            }              



            
workflows:
  my_workflow:
    jobs:
      - my_job:  
          context:
            - SLACK_SECRETS
      - slack/on-hold:
          name: Notify Approval
          context:
            - SLACK_SECRETS          
          filters:
            branches:
              only: test         
              
              #test
              
              

